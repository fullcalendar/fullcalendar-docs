# NOTE: keep in sync with _config.yml's exclude

# jekyll
/_site
/.jekyll-metadata
/.sass-cache

# npm
# (applies to FullCalendar versions in assets too)
node_modules

# rubygems in TravisCI
/vendor

# built assets
/assets/*.*
